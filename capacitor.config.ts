import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.com.jdbsoft.restaurantero.app',
  appName: 'Restaurantero',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
